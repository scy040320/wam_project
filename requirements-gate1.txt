# Install the upstream Cosmos Policy + LIBERO environment separately.
# These are the additional lightweight dependencies used by this add-on.
numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
imageio>=2.31
opencv-python>=4.8
torch>=2.1
transformers>=4.40
pytest>=7.4
